regenerate: true
lang: 'en.yml'
usePlayerLanguage: true
# LEGACY/ENHANCED_LEGACY/MINI_MESSAGE
serializer: MINI_MESSAGE
# OFF/FATAL/ERROR/WARN/INFO/DEBUG/TRACE/ALL
logLevel: INFO

# Points are defined one per file as plugins/InfPoints/points/<key>.yml, see points/primary.yml for every option.
# They can also be listed in a 'point' section here, keyed the same way; a key defined in both places uses this file.
